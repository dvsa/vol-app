<button
 data-module="govuk-button"
 data-prevent-double-click="true"
 id="{{id}}"
 class="{{class}}"
 name="{{action_field_name}}"
 type="submit"
 value="{{value}}"
 data-label="{{label}}"
 {{disabled}}
>{{label}}</button>
