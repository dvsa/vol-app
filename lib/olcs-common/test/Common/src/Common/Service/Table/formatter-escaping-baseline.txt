# Formatters not yet covered by the table escaping contract.
#
# FormatterEscapingInvariantTest calls every formatter directly with a hostile row and
# asserts the value reaches the output escaped. All three sections are asserted, so none
# of them can rot: a formatter that starts leaking fails, one that stops leaking fails as
# a stale entry, and anything moving into or out of [skipped] or [unprobed] fails too.
#
# 146 exercised, 0 leaking, 3 could not be driven. 149 formatters in total.
#
# The leaking list is a to-do list, not a permission slip. It should only ever shrink.

[leaking]

# Values the probe renders but cannot carry a payload into, because the formatter
# requires a number or a parseable date and neither can contain "<script>". The
# column renders; that one value is not asserted.
[unprobed]
ExternalConversationLink.createdOn=date
FeeAmount.name=numeric
InternalConversationLink.createdOn=date
Money.name=numeric
NoteUrl.createdOn=date
TransactionAmount.name=numeric
TransactionFeeAllocatedAmount.name=numeric

# Formatters that still cannot be driven at all. A skip means "unknown", never "safe".
[skipped]
ExternalConversationMessage
InternalConversationMessage
NumberStackValue

# reasons, informational only - not parsed
#   ExternalConversationMessage: TypeError: Common\Service\Table\Formatter\ExternalConversationMessage::getSenderName(): Return value
#   InternalConversationMessage: TypeError: array_map(): Argument #2 ($array) must be of type array, CommonTest\Common\Service\Table\
#   NumberStackValue: TypeError: number_format(): Argument #1 ($num) must be of type int|float, string given
