<?php
$title = $this->translate('permits.page.permits.required.title');
$this->headTitle($title);
?>

<?php echo $this->permitsBackLink(); ?>
<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
        <?php echo $this->formErrors($form); ?>
        <span class="govuk-caption-xl">
            <?php echo $this->escapeHtml($ref); ?>
        </span>
        <div class="govuk-fieldset">
            <legend class="govuk-fieldset__legend govuk-fieldset__legend--xl">
                <h1 class="govuk-fieldset__heading">
                    <?php echo $this->escapeHtml($this->translate('permits.page.permits.required.question')); ?>
                </h1>
            </legend>
            <?php
                $translationParams = [
                    $this->currencyFormatter($ecmtApplicationFee),
                    $this->currencyFormatter($ecmtIssueFee),
                ];

                echo $this->translateReplace('permits.form.permits-required.fee.guidance', $translationParams);
            ?>
            <?php
                if ($showConditionalText) {
                    ?><div class="govuk-inset-text"><?php
                    echo $this->translate('permits.page.no-of-permits.inset');
                    ?></div><?php
                }
            ?>
            <div class="govuk-form-group">
                <?php echo $this->form($form); ?>
            </div>
        </div>
    </div>
</div>
