<?php
    $title = $this->translate('permits.application.submitted.title');
    $this->escapeHtml($this->headTitle($title));
?>

<div class="row ecmt">
    <div class="two-thirds">
        <?php echo sprintf($this->translate('markup-ecmt-application-submitted'), $this->escapeHtml($title), $this->escapeHtml($refNumber)); ?>
        <div class="actions-container new-application--wrapper">
            <p>
                <a href="<?php echo $this->escapeHtml($this->url('permits')); ?>" class="action--primary large">
                    <?php echo $this->escapeHtml($this->translate('Finish')); ?>
                </a>
            </p>
        </div>
    </div>
</div>
