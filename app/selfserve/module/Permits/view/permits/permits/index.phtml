<?php
    $title = 'Permits Home - ';
    $this->headTitle($title);
?>

<div class="page-header__wrapper">
    <h1 class="js-title"><?php echo $this->translate('permits.application.overwiew.title'); ?></h1>
</div>

<div class="row">
    <div class="dashboard two-thirds">
        <div class="tabs">
            <?php
            /* @var \Zend\View\Helper\Navigation\Menu $menu */
            $menu = $this->navigation($this->navigation('navigation')->getContainer()->findBy('id', 'dashboard-licences-applications'))->menu();

            echo $menu->setMinDepth(0)->setMaxDepth(0)
                ->setPartial('partials/tabs-nav');
            ?>
        </div>

        <?php echo $this->flashMessengerAll(); ?>

        <div class="actions-container new-application--wrapper">
            <a href="/permits/ecmt-licence" class="action--primary large">
                <?php echo $this->translate('permits.application.overwiew.button'); ?>
            </a>
        </div>

        <div class="table__header">
            <?php if($issuedNo === 0): ?>
                <h3><?php echo $this->translate('dashboard-table-permits-title'); ?></h3>
                <p class="guidance"><?php echo $this->translate('permits.application.overwiew.no-permits'); ?></p>
            <?php else : ?>
                <?php echo $issuedTable; ?>
            <?php endif; ?>

            <?php if($applicationsNo > 0): ?>
            <?php echo $applicationsTable; ?>
            <?php else : ?>
                <h3><?php echo $this->translate('dashboard-table-permit-application-title'); ?></h3>
                <p class="guidance"><?php echo $this->translate('permits.application.overwiew.no-permits'); ?></p>
            <?php endif; ?>
        </div>
    </div>
    <?php echo $this->partial('partials/dashboard-right-column'); ?>
</div>
