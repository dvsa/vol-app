<?php
    $title = $this->translate('permits.page.title.restricted-countries');
    $this->headTitle($title);
?>

<head>
    <style>
        #restricted-countries-list,
        #restricted-countries label:last-child .restrictedRadio.selected ~ #restricted-countries-list{
            display: none;
        }
        .restrictedRadio.selected ~ #restricted-countries-list {
            display: block;
        }
    </style>
</head>

<div class="page-header">
    <p><a href="/permits/sectors" class="back-link"> <?php echo $this->translate('permits.back-link'); ?> </a></p>
    <div class="page-header__wrapper">
        <h3 class="page-header__subtitle">
            <?php echo sprintf($this->translate('application.steps'), 3, 3); ?>
        </h3>
        <h1 class="js-title">
            <?php echo $this->escapeHtml($title); ?>
        </h1>
        <h2 class="guidance">
            <p><?php echo $this->translate('permits.page.message.restricted-countries'); ?></p>
        </h2>
    </div>
</div>

<?php
    $form->setAttribute('action', $this->url('permits', array('action' => 'overview')));
    $form->prepare();
    echo $this->form()->openTag($form);
    echo $this->formHidden($form->get('id'));
?>

<div class="field">
    <fieldset id="restricted-countries" data-group="restricted-countries">
        <legend><?php echo $this->escapeHtml($this->translate('permits.page.field.restricted-countries')); ?></legend>
        <?php echo $this->formRow($form->get('restrictedCountries')); ?>
        <fieldset id="restricted-countries-list" data-group="restricted-countries-list" class="guidance">
            <p class="hint"><?php echo $this->escapeHtml($this->translate('permits.application.ecmt.select')); ?></p>
            <?php echo $this->formRow($form->get('restrictedCountriesList')); ?>
        </fieldset>
    </fieldset>

    <fieldset class="actions-container new-application--wrapper">
        <p><?php echo $this->formSubmit($form->get('submit'));  ?></p>
        <p><a href="/permits"> <?php echo $this->translate('cancel'); ?> </a></p>
    </fieldset>
</div>

<?php echo $this->form()->closeTag(); ?>
