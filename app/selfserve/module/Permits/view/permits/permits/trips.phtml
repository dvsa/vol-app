<?php
$title = $this->translate('permits.page.number-of-trips.title');
$this->headTitle($title);
?>

<?php echo $this->permitsBackLink(); ?>
<?php echo $this->formErrors($form); ?>
<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
        <span class="govuk-caption-xl">
            <?php echo $this->escapeHtml($ref); ?>
        </span>
        <div class="govuk-fieldset">
            <legend class="govuk-fieldset__legend govuk-fieldset__legend--xl">
                <h1 class="govuk-fieldset__heading">
                    <?php echo $this->escapeHtml($this->translate('permits.page.number-of-trips.question')); ?>
                </h1>
            </legend>
            <?php if ($form->get('Fields')->get('intensityWarning')->getValue() === 'yes'): ?>
                <?php echo $this->partial('partials/warning-component', ['translationKey' => 'permits.form.trips.warning']); ?>
            <?php endif; ?>

            <?php if ($isNI): ?>
                <?php echo $this->partial('partials/warning-component', ['translationKey' => 'permits.page.number-of-trips.northern-ireland.warning']); ?>
            <?php endif; ?>

            <?php echo $this->translate('markup-ecmt-trips-hint.phtml'); ?>

            <div class="govuk-form-group">
                <?php echo $this->form($form); ?>
            </div>
        </div>
    </div>
</div>
