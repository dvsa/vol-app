<?php
    $title = $this->translate('permits.page.number-of-trips.title');
    $this->escapeHtml($this->headTitle($title));
?>

<div class="row ecmt">
    <div class="two-thirds">
        <div class="page-header">
            <p><?php echo $this->permitsBackLink(); ?></p>
            <div class="page-header__wrapper">
                <h3 class="page-header__subtitle"><?php echo $this->escapeHtml($ref); ?></h3>
                <h1 class="js-title">
                    <?php echo $this->escapeHtml($this->translate('permits.page.number-of-trips.question')); ?>
                </h1>
                <p>
                    <?php
                        // Only show if the Licence Traffic Area is 'Northern Ireland'
                        if ($this->trafficAreaId === 'N') {
                            echo $this->escapeHtml($this->translate('permits.page.number-of-trips.northern-ireland'));
                        }
                    ?>
                </p>
            </div>
        </div>

        <div class="field">

            <fieldset id="number-of-trips" class="inline" data-group="number-of-trips">
                <?php
                    echo $this->formErrors($form);
                    echo $this->form($form);
                ?>
            </fieldset>
        </div>

    </div>
</div>
