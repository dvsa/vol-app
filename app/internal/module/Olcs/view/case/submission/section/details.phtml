<h4><?php
    echo isset($this->data['data']['description']) ? $this->data['data']['description'] :
        '';
    ?>
    <a href="<?php echo $this->url(
        'submission', [
            'case' => isset($this->data['data']['id']),
            'action' => 'edit',
        ]
        );?>" class="action--secondary right-aligned">
        <?php echo
        $this->translate('Edit section');
        ?></a>
</h4>
<div class="read-only">
    <dl>
        <?php
        foreach ($this->data['data'] as $k => $v) {
        ?>
        <dt><?php echo $this->translate($k);?></dt>
        <dd><?php echo $v;?></dd>

        <?php
        }
        ?>
    </dl>
</div>

