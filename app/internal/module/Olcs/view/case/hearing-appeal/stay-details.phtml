<?php
if (!empty($id))
{
    $action = 'Edit';
} else {
    $action = 'Add';
}
?>
<h4><?php echo $this->translate(
        $stayType['description']
    ); ?>
    <a href="<?php echo $this->url(
        'case_stay',
        [
            'case' => $case['id'],
            'action' => strtolower($action),
            'stayType' => $stayType['id'],
        ], [], false); ?>" class="action--secondary
        right-aligned"><?php echo $this->translate($action);
        ?></a>
</h4>

<?php if ($action == 'Edit') { ?>
    <div class="read-only">
        <dl>
            <dt><?php echo $this->translate('Stay type');?></dt>
            <dd><?php echo isset($stayType['description']) ? $stayType['description'] : ''; ?></dd>

            <dt><?php echo $this->translate('Outcome');?></dt>
            <dd><?php echo isset($outcome['description']) ? $outcome['description'] : ''; ?></dd>

            <dt><?php echo $this->translate('Requested date');
                ?></dt>
            <dd><?php echo isset($requestDate) ? $requestDate : ''; ?></dd>

            <dt><?php echo $this->translate('Decision date');?></dt>
            <dd><?php echo isset($decisionDate) ? $decisionDate : ''; ?></dd>
            <dt><?php echo $this->translate('Is withdrawn');?></dt>
            <dd><?php echo $this->translate(empty($withdrawnDate) ?
                    'N' : 'Y'); ?></dd>
            <dt><?php echo $this->translate('Withdrawn date');?></dt>
            <dd><?php echo isset($withdrawnDate) ? $withdrawnDate : ''; ?></dd>
        </dl>
        <dl>
            <dt><?php echo $this->translate('Notes');?></dt>
            <dd class="three-quarters"><?php echo
                isset($notes) ? $notes : '';
                ?></dd>
        </dl>
    </div>

<?php
}
?>
