<?php
$table = $this->placeholder('table')->getValue();

if (is_object($table)) {
    echo $table;
} elseif ($table !== []) {
    echo $this->render('/placeholder/placeholder',
        [
            'header' => 'Placeholder',
        ]
    );
}
?>
