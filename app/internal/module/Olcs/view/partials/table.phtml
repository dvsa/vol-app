<?php

if (!isset($table)) {
    $table = $this->placeholder('table')->getValue();
}

if (!empty($table)) {
    if (!empty($readonly)) {
        $table->setDisabled(true);
    }

    $rendered = $table->render();

    echo $rendered;
}
