<?php
$placeholderPageTitle = $this->placeholder('pageTitle')->getValue();
if (is_array($placeholderPageTitle)) {
    $placeholderPageTitle = reset($placeholderPageTitle);
}
?>

<div class="js-response">
    <div class="">
        <?php if (isset($this->pageTitle)):
            echo $this->translate($this->pageTitle);
        elseif ($placeholderPageTitle):
            echo $this->translate($placeholderPageTitle);
        endif;
        ?>
    </div>
    <div class="js-body">
        <div class="js-content">
            <?php echo $this->content; ?>
        </div>

        <div class="js-script">
            <?php echo $this->inlineScript() ?>
        </div>
    </div>
</div>
