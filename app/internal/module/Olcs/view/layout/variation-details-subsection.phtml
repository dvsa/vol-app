<div class="one-fifth--left">
    <h4><?php echo $this->translate('variation-details-nav'); ?></h4>
    <ul class="app-navigation" role="menu">
        <?php foreach ($sections as $reference => $details) : ?>
            <?php $current = ($details['route'] === $currentRoute); ?>
            <li class="app-navigation__item <?php echo $current ? 'current' : $details['class']; ?>">
                <?php if (!$current) : ?>
                    <a href="<?php echo $this->url($details['route'], array(), array(), true); ?>">
                <?php endif; ?>
                <?php echo $this->translate('section.name.' . $reference); ?>
                <?php if (!$current) : ?>
                    </a>
                <?php endif; ?>
                <?php if ($current && isset($filterForm)): ?>
                    <?php echo $this->form($filterForm); ?>
                <?php endif;?>
            </li>
        <?php endforeach; ?>
    </ul><!-- end .progress -->
</div>
<div class="three-fifths">
  <?php echo $this->flashMessengerAll(); ?>
  <?php echo $this->content; ?>
</div>
