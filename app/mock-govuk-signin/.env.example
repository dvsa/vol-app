# Mock GOV.UK Sign In Service Configuration

# Server
PORT=8080
NODE_ENV=development

# Validation Mode
# Options: permissive (default), standard, strict
VALIDATION_MODE=permissive

# OAuth Configuration
ISSUER_URL=http://localhost:8080

# Logging
LOG_LEVEL=debug

# JWT Keys (auto-generated in permissive mode if not provided)
# JWT_PRIVATE_KEY=
# JWT_PUBLIC_KEY=